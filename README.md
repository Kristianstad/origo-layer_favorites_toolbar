# origo-layer_favorites
Layer favorites plugin for Origo. Adds a slide-down bar at the top of the window where you can save and restore visible map layers.

Initialize with:
´´´origo.api().on('load', initLayerFavoritesToolbar(););´´´
